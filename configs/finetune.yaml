model:
  target: spamo.t5_slt.FlanT5SLT
  
  params:     
    lr: 6.0e-4
    monitor: val/bleu4
    model_name: google/flan-t5-small
    cache_dir: /data3/models
    inter_hidden: 512
    input_size: 2048
    beam_size: 5
    max_frame_len: 512
    max_txt_len: 64
    frame_sample_rate: 1
    prompt: "Translate the given sentence into {}."
    tuning_type: lora
    lora_r: 64
    lora_alpha: 128
    lora_dropout: 0.1
    fusion_mode: joint
    warm_up_steps: 0
    cross_modal_align: true
    combined_loss: true
    alpha: 1.0
    use_in_context: false
    num_in_context: 0

lightning:
  callback:
    learning_rate_logger:
      target: pytorch_lightning.callbacks.LearningRateMonitor
      params:
        logging_interval: step
  trainer:
    strategy: ddp_find_unused_parameters_true
    accumulate_grad_batches: 2
    accelerator: gpu
    devices: 1
    max_epochs: 1
    gradient_clip_val: 1.0
    check_val_every_n_epoch: 2
    precision: bf16

data:
  target: dataset.datamodule.DataModuleFromConfig
  params:
    batch_size: 8
    num_workers: 4
  
    train:
      target: dataset.how2sign.How2Sign
      params:
        anno_root: ./preprocess/How2Sign
        vid_root: /kaggle/input/datasets/guangwoen/how2sign-clips/how2sign_clips/train_clips_raw_videos
        feat_root: /kaggle/input/datasets/mohamedmoibrahim/spamo-how2sign-spatial-features
        mae_feat_root: /kaggle/input/private-dataset/how2sign_motion_features
        mode: "train"
        spatial: true
        spatiotemporal: true
        spatial_postfix: "_None_s2wrapping"
        spatiotemporal_postfix: "_None_overlap-8"
        
    validation:
      target: dataset.how2sign.How2Sign
      params:
        anno_root: ./preprocess/How2Sign
        vid_root: /kaggle/input/datasets/guangwoen/how2sign-clips/testing/testing/test_clips_raw_videos
        feat_root: /kaggle/input/datasets/mohamedmoibrahim/spamo-how2sign-spatial-features
        mae_feat_root: //kaggle/input/private-dataset/how2sign_motion_features
        mode: "val"
        spatial: true
        spatiotemporal: true
        spatial_postfix: "_None_s2wrapping"
        spatiotemporal_postfix: "_None_overlap-8"

    test:
      target: dataset.how2sign.How2Sign
      params:
        anno_root: ./preprocess/How2Sign
        vid_root: /kaggle/input/datasets/guangwoen/how2sign-clips/testing/testing/test_clips_raw_videos
        feat_root: /kaggle/input/datasets/mohamedmoibrahim/spamo-how2sign-spatial-features
        mae_feat_root: /kaggle/input/private-dataset/how2sign_motion_features
        mode: "test"
        spatial: true
        spatiotemporal: true
        spatial_postfix: "_None_s2wrapping"
        spatiotemporal_postfix: "_None_overlap-8"